# 0.9.2

- Keeps already-projected file and task reminders in the signed TishOS schedule when optional external-calendar discovery fails.
- Prevents one external source problem from making the entire paired TishOS reminder queue appear empty.
- Adds a privacy-bounded failure log and a regression for an hours-overdue five-minute repeat whose rule also accepts external events.

This is a backward-compatible Controller patch with no settings or note-data migration. Minimum supported Obsidian remains 1.12.0.

Final validation passed the 60-check focused reminder suite with three preserved historical comparison skips, the complete declared suite, and the mandatory separate production-mode build. The final build reported `[runtime-deploy] target=test ... unchanged`; **Obsidian Plugin Test Vault** alone was reloaded and loaded runtime manifest 0.9.2. Source and test-runtime artifacts are byte-identical: `main.js` SHA-256 `aed05cc034148744d2b5e99160b82681f6ad78e1c411b86d7a70b87ce4c97f0f`, `manifest.json` `64165a25bf2966208885aec503c4efa175637aa0bb87f75371a7cc604abcf8bc`, and `styles.css` `2da24f2a872483deb0ccc2944fb3b05c1b8a4e6b9f90b3290a3fabb86ec06999`. Production was read only for bounded diagnosis and was not changed or promoted.
