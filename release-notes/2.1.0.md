# TPS Controller 2.1.0

Advanced calendar field names now use one draft input and Apply. GCM previews and confirms migration before saving the new key. Matching enabled Calendar, Controller, Health and GCM mappings follow together. Calendar synchronization waits while a shared property migration is writing.

Minor release: adds reviewed mapping changes without changing existing defaults. Existing settings routes, defaults, disclosures and commands remain. No migration runs automatically on upgrade; no fallback alias editor is added.

## Migration and compatibility

Enable/update TPS Global Context Menu 3.0.0 before applying shared Controller or Calendar key changes. Enable participating TPS plugins before a shared rename. Use the explicit reviewed migration to consolidate historical notes before relying on their old fields. Cancel preserves both mappings and notes. Conflicting destination properties and stale previews stop the change. Historical names are used only for explicit migration or identity protection, not ordinary data reads.

Changes cover Markdown frontmatter, including archives and templates. Bodies are preserved. Inline fields, Base formulas, per-view Base mappings and disabled plugins are not automatically rewritten. Health uses guarded rollback; GCM retains a temporary private recovery copy. Neither can make a vault-wide transaction atomic across a process crash or simultaneous edits on another device. Repair potentially relevant malformed frontmatter before migrating.

Minimum Obsidian: **1.12.3**. GCM and Health major versions intentionally remove legacy read compatibility.

## Validation and handoff

`npm test`: **509 passed, 3 existing/optional skips, zero failures**. TypeScript and a separate final `npm run build` passed. The shared deployment helper reported `target=test`; runtime artifacts matched the source build byte-for-byte and existing runtime settings were preserved. All four affected test-vault plugins were reloaded at their final versions.

Actual test-vault UI QA cancelled and then confirmed a Controller key change against one synthetic note, updating both Controller and Calendar. The body stayed unchanged, input focus returned, and the recovery file was removed. Original settings were restored and the fixture archived. GCM's single kind-key editor, Calendar's five key inputs/Apply actions, and Health's timing controls were checked. Health correctly rejected an archived malformed QA note with no writes; successful Health migration, conversion, cancellation and rollback have focused regression coverage. Physical iOS acceptance remains user testing. No outbound automation was enabled.

Tested in **Obsidian Plugin Test Vault** and ready for the user's **BRAT pull**. Production was not deployed or reloaded. Existing dirty canonical worktrees were preserved; this change was isolated from published main.

## Artifact SHA-256

| Artifact | SHA-256 |
| --- | --- |
| `main.js` | `3527b8aef83626fecde256c1a432cd91a08a4583e0babd26e7ec99abe1ce8fd6` |
| `manifest.json` | `8665c45145b5bdd61e271318a05a60cbe219f967339f41a68c0aa55b7fd2a47d` |
| `styles.css` | `52e5093d45e2f22ab93a84298cd3c4377b8c6eefef4921df8d58c7ea625b15e9` |
| `styles-ui.css` | `2a7d52906fcab24139f0a3831f334d07aea0280acac4e27820b6ee102f28fe0a` |
